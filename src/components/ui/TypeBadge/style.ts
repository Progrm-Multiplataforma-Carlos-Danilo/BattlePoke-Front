import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  badge: {
    paddingHorizontal: 6,
    paddingVertical: 2,
    borderRadius: 4,
  },
  label: {
    color: "#FFF",
    fontSize: 8,
    fontWeight: "bold",
    textTransform: "uppercase",
  },
});
